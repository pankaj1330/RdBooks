# Ensure you have installed node in your system
# open terminal 
# go to pdfreader folder

# run command `npm install`
# run command `npm run dev`
# open the link generated by terminal to your local computer
